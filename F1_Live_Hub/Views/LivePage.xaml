<Window x:Class="F1_Live_Hub.Views.LivePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Title="F1 Live Hub - Live"
        Height="600" Width="380"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Border CornerRadius="16" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0F0F0F" Offset="0"/>
                <GradientStop Color="#1A0000" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ═══ HEADER POPUP ═══ -->
            <Border Grid.Row="0" Background="#0F0F0F"
                    BorderBrush="#2A2A2A" BorderThickness="0,0,0,1"
                    Padding="16,14" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#E8002D" CornerRadius="6"
                                Width="32" Height="32" Margin="0,0,10,0">
                            <TextBlock Text="🤖" FontSize="16"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="F1 CHATBOT" Foreground="White"
                                       FontSize="15" FontWeight="Bold"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Fill="#00CC00" Width="6" Height="6"
                                         VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="Assistant IA en ligne" Foreground="#888888" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#2A0000" CornerRadius="8"
                            Padding="10,6" Cursor="Hand"
                            MouseLeftButtonDown="BtnFermer_Click">
                        <TextBlock Text="✕" Foreground="#E8002D" FontSize="14"
                                   FontWeight="Bold" HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- ═══ ZONE MESSAGES ═══ -->
            <ScrollViewer Grid.Row="1" x:Name="ScrollMessages"
                          VerticalScrollBarVisibility="Auto"
                          Background="Transparent" Padding="12,10">
                <StackPanel x:Name="MessagesPanel">

                    <!-- Message de bienvenue IA -->
                    <StackPanel Margin="0,0,0,12" HorizontalAlignment="Left" MaxWidth="280">
                        <Border Background="#1A1A1A" CornerRadius="4,12,12,12"
                                Padding="12,10">
                            <StackPanel>
                                <TextBlock Foreground="#E8002D" FontSize="10"
                                           FontWeight="Bold" Margin="0,0,0,4">
                                    🏎 Assistant F1
                                </TextBlock>
                                <TextBlock Text="Bonjour ! Je suis ton assistant F1. Pose-moi tes questions sur les pilotes, les courses, les classements ou tout ce qui concerne la Formule 1 !"
                                           Foreground="White" FontSize="12"
                                           TextWrapping="Wrap" LineHeight="18"/>
                            </StackPanel>
                        </Border>
                        <TextBlock Text="Maintenant" Foreground="#555555"
                                   FontSize="9" Margin="4,4,0,0"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- ═══ ZONE SAISIE ═══ -->
            <Border Grid.Row="2" Background="#0F0F0F"
                    BorderBrush="#2A2A2A" BorderThickness="0,1,0,0"
                    Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#1A1A1A" CornerRadius="20"
                            BorderBrush="#333333" BorderThickness="1" Padding="14,10">
                        <TextBox x:Name="TxtMessage"
                                 Background="Transparent" Foreground="White"
                                 FontSize="13" BorderThickness="0"
                                 CaretBrush="White" AcceptsReturn="False"
                                 VerticalAlignment="Center"
                                 KeyDown="TxtMessage_KeyDown">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Grid>
                                                    <TextBlock Text="Pose ta question F1..."
                                                               Foreground="#555555" FontSize="13"
                                                               VerticalAlignment="Center"
                                                               Margin="2,0,0,0">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Border>
                    <Border Grid.Column="2" Width="42" Height="42"
                            CornerRadius="21" Cursor="Hand"
                            MouseLeftButtonDown="BtnEnvoyer_Click">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF0028" Offset="0"/>
                                <GradientStop Color="#E8002D" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#E8002D" BlurRadius="10"
                                             Opacity="0.4" ShadowDepth="0"/>
                        </Border.Effect>
                        <TextBlock Text="➤" Foreground="White" FontSize="16"
                                   FontWeight="Bold" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Margin="2,0,0,0"/>
                    </Border>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>